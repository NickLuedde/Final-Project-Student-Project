@model IEnumerable<GAG_101.Models.JobTypes1>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name_JobType)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name_JobType)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.JobType_ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.JobType_ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.JobType_ID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
